<%= react_component("GitHubCallback", {
    provider: @provider,
    github_uid: @github_uid,
    name: @name,
    image: @image,
    description: @description,
    url: @url,
    github_url: @github_url,
    location: @location
}) %>